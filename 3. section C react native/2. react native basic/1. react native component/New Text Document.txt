-	Mobile and pc should be in the same wifi.
-	View component is just like Div, text is working like paragraph. 
-	<View style={styles.container}>
-	      <Text>Hellow World!</Text>
-	      <StatusBar style="auto" />
-	    </View>
-	Go to react native official website -> component https://reactnative.dev/docs/components-and-apis
-	Now use textInput as input field.
-	onChange(will accept a function)
app.js:
import { StatusBar } from 'expo-status-bar';
import React from 'react';
import { StyleSheet, Text, View, TextInput  } from 'react-native';

export default function App() {
  const handleInputChange = text => {
    alert(text);
  }
  return (
    <View style={styles.container}>
      <Text>Hellow World!</Text>
      <TextInput
        style={{height: 40, borderColor: 'gray', borderWidth: 1}}
        placeholder="I am a place Holder"
        onChangeText = {text => handleInputChange(text)}
        />
      <StatusBar style="auto" />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});

